### Oatput UI Updates

## Setup flow
- "Connect Slack" screen needs updates, it's pretty ugly. 
- "Connect Slack" issue > prompt says: "Find it in Slack: click your name → View full profile → copy Member ID." But should be: "Find it in Slack: click your picture/name at the bottom left > Profile > Ellispes > copy member ID 
- "Connect Granola" same issue as slack: "Open Granola > Preferences > API > Generate New API Key > Select Personal (Or Enterprise??? Unsure if this is required, went with Personal for now)
- How do we handle workspace selection? Check out Trailmix handles this

## General
Default to darker brown color vs. the light/papyrus color of the app right now.

## Exits
- Need a way to break a previous integraiton/setup, currently you can't "stop" Oatput from running
- Need a way to change the Monday board that is being action items - i.e. go from board a to board b, same thing for selecting workspaces I guess too? 
